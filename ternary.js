//Ternary If Statement

var number = 2; //the  number  1 will generate an odd number

//ternary statement
var result = number % 2 == 0 ? "Even" : "Odd";

//if-statement
if (number % 2 == 0) {
  console.log("the number is even");
  //A number that is divisible by 2 and generates a remainder of 0 is called an even number
} else {
  console.log("the number  is odd");
  //An odd number is a number that is not divisible by 2. The remainder in the case of an odd number is always “1”.
}
